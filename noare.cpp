#include <iostream>
using namespace std;
int main()
{
    int x;
    cout<<"enter x";
    cin>>x;
    if(x>0)
    cout<<"its a positive no";
    if(x<0)
    cout<<"its a negative no";
    else
    cout<<"zero";
}